apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: skynetx-app-hpa
  namespace: skynetx
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: skynetx-app
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
