apiVersion: v1
kind: Service
metadata:
  name: skynetx-service
  namespace: skynetx
spec:
  type: NodePort
  selector:
    app: skynetx-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
