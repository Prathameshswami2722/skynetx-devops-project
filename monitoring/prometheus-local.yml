global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/alert-rules.yml

scrape_configs:
  - job_name: "skynetx-app"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "host.docker.internal:5002"
